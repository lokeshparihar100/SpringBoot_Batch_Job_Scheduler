CREATE TABLE datasource2.name (
    first_name VARCHAR(50),
    last_name VARCHAR(50)
);

CREATE TABLE datasource2.details (
    address VARCHAR(50),
    mobile_number int8
);

CREATE TABLE datasource2.job (
    job_title VARCHAR(50),
    company VARCHAR(50),
    role VARCHAR(20)
);
